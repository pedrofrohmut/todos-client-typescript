import styled from "styled-components"

export const Container = styled.div`
  color: var(--success);
  font-size: 1.2rem;
  letter-spacing: 0.5px;
  display: block;
  padding: 12px 0;
  text-align: center;
`
